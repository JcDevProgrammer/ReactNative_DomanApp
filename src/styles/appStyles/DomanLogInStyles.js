import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingLeft: 40
      },
    
      linkRegister: {
        paddingLeft: 100,
        color: 'blue'
      }

})