import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    textContainer: {
        paddingTop: 25,
        alignItems: 'center',
        justifyContent: 'center',
        flexDirection: 'row',
      },
    
      linkRegister: {
        paddingLeft: 120,
        color: 'blue'
      }
})